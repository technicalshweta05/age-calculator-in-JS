# age-calculator-in-JS
Built an Age Calculator using JavaScript that computes exact age in years, months, days, hours, minutes, and seconds from user's birth date/time. Uses Date objects and millisecond conversions with leap year adjustment.
